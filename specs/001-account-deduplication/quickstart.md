# Quickstart

No new setup commands required. This feature primarily relies on modifying the existing `auth.service.ts` configuration.

Validation notes:
- Use existing auth flows to test merge-on-login and unlink safeguards.
- Do not use Supertokens AccountLinking recipe or related imports.
