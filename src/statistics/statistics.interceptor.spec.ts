import { StatisticsInterceptor } from "./statistics.interceptor";

describe("StatisticsInterceptor", () => {
    it("should be defined", () => {
        expect(new StatisticsInterceptor()).toBeDefined();
    });
});
