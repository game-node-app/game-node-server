export enum StatisticsSourceType {
    GAME = "game",
    REVIEW = "review",
    ACTIVITY = "activity",
    COLLECTION = "collection",
}

export enum StatisticsActionType {
    INCREMENT = "increment",
    DECREMENT = "decrement",
}
