export enum ENotificationCategory {
    LIKE = "like",
    COMMENT = "comment",
    LAUNCH = "launch",
}

export enum ENotificationSourceType {
    GAME = "game",
    REVIEW = "review",
    ACTIVITY = "activity",
}
