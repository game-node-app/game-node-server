/**
 * Where the game data is stored in the GameNode system.
 * Helps the clients determine the source of the data.
 */
export enum EGameStorageSource {
    MYSQL = "MYSQL",
    MANTICORE = "MANTICORE",
}
